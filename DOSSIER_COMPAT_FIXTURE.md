# Dossier compatibility fixture

Pinned commit in the private `content-agn` repository (`cirsteve/algerknown-content`)
used as the Algerknown adapter compatibility fixture:

```
ca3f0b73fe7458c8c391ce10800eb409e19c9faa
```

Commit message: `fix(dossier): align approved compatibility fixtures`

## Validation

```
$ agn validate   # run from the content-agn repository root
```

Result: `agent-evals-dossier` and `agent-ops-dossier` both valid, 0 errors.
